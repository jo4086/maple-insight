# 현재 발견된 문제점

### 1. 관계 맺은 컬럼 삭제시 소스키까지 삭제되는 문제

타겟테이블의 FK키 삭제시 소스키의 PK가 삭제되는 문제 발생
소스키의 PK삭제로 인해 타겟테이블의 FK키는 삭제되는게 맞지만

그 반대인 FK키 삭제로인해 소스키의 PK키는 삭제되어선 안됌

### 2. 식별관계를 포함한 테이블에게 외래키를 받을 때 정보가 포함되어있지 않음.

A가 B에게 식별 외래키를 주고나서
B가 C에게 식별/비식별 외래키를 줄 때에는
C는 2키가 2개 생성되어야함 (B원본PK와 A에게 받은 식별외래키)
