// INFO: 자동 생성 파일 (수정시 주의)

// WARN: 자동 생성 데이터 ↓ (수정 금지)
export type DomainType = 'character';

// WARN: 자동 생성 데이터 ↑ (수정 금지)

// 추가 수동 타입은 아래에 작성 가능

export type CacheValue = unknown;
export interface SetCacheProps {
  key: string;
  value: CacheValue;
}
